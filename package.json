{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "FORCE_COLOR=1 lerna run start --parallel",
    "start:color": "lerna exec -- yarn start"
  }
}
